{
  "name": "open-fpl",
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "inti:data": "yarn --cwd packages/data prebuild",
    "dev:app": "yarn --cwd packages/app dev --port=3000",
    "dev:data": "yarn --cwd packages/data dev --port=3001"
  }
}
